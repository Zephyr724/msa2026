# Community free-stock image replacement

## Actual human instruction

> comminity有很多相同的图，不要有那么多相同的图，用免费版权的，适当主题的图替换，不要再生成了，我没额度了。
>
> 新建分支做

## Implementation instruction

Create a dedicated fix branch. Replace the visually repetitive generated
Community feed catalogue without invoking image generation. Use distinct,
theme-appropriate stock photographs whose free-use terms and original sources
can be verified. Bundle the images for deterministic production and Development
parity, update truthful alt text, retain a source/licence register, preserve
unrelated working-tree changes, and run focused Community seed and frontend
verification. Do not change schema, dependencies, authentication, or product
scope, and do not stage or commit.

Follow-up correction: eliminate repeated image URLs across the 44-post
Development feed. Do not rely only on distinct file hashes. Reconcile existing
deterministic rows, keep story/image subjects reasonably aligned, reuse
existing project-owned Quest illustrations where appropriate, and verify the
no-repeat rule in the persistence test.

Second follow-up after failed visual verification: remove all Quest SVG
substitutes from Community cards, add enough real free-stock photos for the
Development feed, restart the actual local backend so deterministic rows are
reconciled, and inspect the rendered Community page in a browser. Completion
requires zero repeated image URLs and zero SVG covers in the loaded feed.
