# Passport XP progress simplification

## Reconstructed implementation instruction

Simplify the Passport summary card's XP progress presentation because the
current XP fraction is repeated above and below the progress bar. Keep the
top-line fraction, progress bar, and remaining XP message; remove only the
redundant visible sentence. Preserve a complete accessible value description
on the progress bar, update regression tests, and run the applicable frontend
gates. Work on a short-lived branch rather than directly on `main`. Do not
stage, commit, push, or create a pull request without explicit human approval.
