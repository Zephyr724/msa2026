# Slice 19 Completion History Additive Implementation Prompt

Restore Passport `Completion history` to the checked-in Figma Make reference
under `docs/UI/Kiwimpact MVP UI Design/src/app/App.tsx`, while preserving the
accepted Passport authority and privacy boundaries.

Implement the Make heading, share-card action, explanatory copy, status pills,
six circular category filters, responsive two-column/one-column history grid,
image-led completion cards, verified/self-reported overlay, XP or
Passport-only state, category badge, achievement badge, and per-completion
Share action.

Do not hard-code visual-only completion data. Extend the authenticated
read-only Passport history contract with the Quest cover image and achievement
names linked to the authoritative XP transaction. The Share action must open
the existing builder with the selected verified completion.

Do not add a dependency, role, schema migration, or new authorization path.
Update strict DTO validation and regression tests. Run targeted and applicable
full gates, verify desktop/mobile rendering and filter interaction in the local
browser, update the Slice 19 completion report, and obtain one independent K3
read-only review of this additive diff before requesting commit approval.
