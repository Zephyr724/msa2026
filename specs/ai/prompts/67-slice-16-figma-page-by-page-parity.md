# Slice 16 — Implementation Instruction

Implement the accepted
`specs/implementation/16-figma-page-by-page-parity.md` contract.

Use the connected Figma Make source for file
`2pACGo2MnSp1BOFglrkFJ0`, node `0:1`, the runnable local Make export, and the
stored Slice 14 prototype screenshots as the design source. Work page by page
in the accepted order:

1. global scale and shell;
2. Landing;
3. Discover;
4. Quest Detail;
5. Mission Board;
6. Passport;
7. Leaderboard;
8. Share Card;
9. completion chooser and reward.

For each page, preserve authoritative production behaviour and tests, then
match the Make section order, first-fold composition, type scale, spacing,
column proportions, card density, primary-action hierarchy, and mobile
reordering. Do not copy demo values or remote/unlicensed imagery.

Do not mark a page complete from source inspection or a full-page smoke
screenshot. Capture Make and production at the same CSS viewport, compare the
viewport images, correct visible differences, and only then continue. Keep
viewport and full-page evidence distinct. Reject captures with clipping,
repetition, unexpected blank bands, or mismatched device scale.

Run targeted tests during implementation and the applicable full verification
gates at closure. Record only observed results. Create the completion report
and evidence manifest before requesting one independent K3 read-only review.
